---
phase: 02-financial-advisor
plan: 07
type: execute
wave: 4
depends_on: [02-05, 02-06]
files_modified: []
autonomous: false
requirements: [FIN-01, FIN-02, FIN-03, FIN-04, FIN-05, FIN-06]
must_haves:
  truths:
    - "First time Finance panel opens, onboarding conversation runs with warm reflective tone"
    - "User can skip onboarding and reach Dashboard immediately"
    - "User can upload a CIBC CSV (chequing or credit card) and see row count feedback"
    - "Dashboard shows 4 glass chart sections: category bars, budget vs actual, monthly trend, transaction list"
    - "Category bars animate in with CSS transition; dominant bar has accent glow"
    - "Transaction list shows credits in green with up-arrow tag; debits in default color"
    - "Period selector filters all 4 chart sections to the selected period"
    - "Multiple CSV uploads accumulate correctly; all period labels appear in period selector"
    - "Finance Chat tab accepts a question and streams an answer grounded in SQL data"
    - "Finance panel is accessible without model loaded (Dashboard is SQL-only)"
    - "Reset Goals clears onboarding and re-triggers the onboarding flow on next open"
    - "Finance panel opens and closes without layout breakage; main chat is unaffected"
  artifacts:
    - path: "app/finance.py"
      provides: "All 6 FIN requirements implemented"
    - path: "app/static/js/app.js"
      provides: "financeUI IIFE complete with all interaction logic"
    - path: "app/templates/index.html"
      provides: "Finance panel HTML with all required IDs"
    - path: "app/static/css/app.css"
      provides: "All .fin-* styles matching Midnight Glass design"
  key_links:
    - from: "Finance panel"
      to: "SQL data only"
      via: "All numbers come from /finance/dashboard_data SQL queries — zero LLM for numbers"
      pattern: "dashboard_data"
    - from: "Finance Chat"
      to: "LLM with SQL context"
      via: "POST /finance/chat injects build_finance_context() into system prompt"
      pattern: "FINANCIAL CONTEXT"
---

<objective>
Human sign-off on the complete Financial Advisor feature. All 6 requirements verified through live browser interaction with real CIBC CSV data.

Purpose: Gate before moving to Phase 3. Claude cannot verify visual polish, animation feel, or UX flow quality.
Output: Human approval of all 4 success criteria.
</objective>

<execution_context>
@/home/rishi/.claude/get-shit-done/workflows/execute-plan.md
@/home/rishi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-financial-advisor/02-CONTEXT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and start server</name>
  <files></files>
  <action>
    Run the complete finance test suite to confirm all automated verifications pass:

    ```bash
    cd /home/rishi/Rishi/AI/Localis
    python -m pytest tests/test_finance*.py -v 2>&1
    ```

    Then start the development server:
    ```bash
    uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
    ```

    Report the test results before the checkpoint.
  </action>
  <verify>
    <automated>cd /home/rishi/Rishi/AI/Localis && python -m pytest tests/test_finance*.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>All finance tests pass (0 failures, 0 errors). Server starts without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Financial Advisor feature</name>
  <action>Human manually verifies all 6 FIN requirements through live browser interaction with real CIBC CSV data. Server must be running from Task 1.</action>
  <what-built>
    Complete Financial Advisor feature:
    - FIN-01: Goal-setting onboarding conversation (4 questions, skippable, re-runnable)
    - FIN-02: CIBC CSV upload (chequing + credit card, period label, encoding-safe)
    - FIN-03: Deterministic categorisation (Food/Transport/Shopping/Utilities/Entertainment/Other)
    - FIN-04: Glass dashboard with 4 chart sections (CSS bars, no external libraries)
    - FIN-05: Multi-period accumulation with period selector dropdown
    - FIN-06: Finance Chat tab with SQL context injection (no raw CSV to LLM)
  </what-built>
  <how-to-verify>
    Open http://localhost:8000 in browser. Verify these scenarios:

    **Scenario 1 — Onboarding (FIN-01)**
    1. Click Finance icon in the right sidebar collapsed rail
    2. Finance panel opens — onboarding conversation starts automatically
    3. Verify warm reflective tone ("What are you saving toward?" not a form)
    4. Answer all 4 questions (goal type, life events, budgets form, time horizon)
    5. Budget step should show an inline form card with 6 category inputs
    6. After completing, verify transition to Dashboard tab

    **Scenario 2 — Skip onboarding**
    1. Click "Reset goals" button at top of Finance panel → confirm reset
    2. Finance panel re-opens to onboarding
    3. Click "Skip setup" → verify Dashboard appears with empty state

    **Scenario 3 — CSV Upload (FIN-02, FIN-03)**
    1. Click "Upload CSV" button in Dashboard period bar
    2. Select a CIBC chequing CSV file
    3. Enter period label "Jan 2026" → click Upload
    4. Verify upload status shows row count and "0 duplicates skipped"
    5. Upload the same file again → verify "N duplicates skipped" (FIN-05 dedup)
    6. Upload a second CSV with a different period label (e.g. credit card, "Feb 2026")

    **Scenario 4 — Dashboard (FIN-04, FIN-05)**
    1. After uploads, verify 4 sections visible: Spending by Category, Budget vs Actual, Monthly Trend, Transactions
    2. Category bars should animate in with CSS transition
    3. Highest-spend category bar should have a subtle glow
    4. Transaction list: debits normal color, credits in green with "↑ Credit" tag
    5. Change period selector → charts should update to show filtered data
    6. "All time" should aggregate across all uploaded periods

    **Scenario 5 — Finance Chat (FIN-06)**
    1. Click "Chat" tab in Finance panel
    2. Type: "What did I spend the most on?"
    3. Verify streaming response with specific dollar amounts matching your CSV data
    4. Type: "How close am I to my food budget?"
    5. Verify LLM mentions budget vs actual numbers (not invented)

    **Scenario 6 — No model required for Dashboard**
    1. Note whether Dashboard works without a model loaded (panel should show SQL data regardless)

    **Visual checks:**
    - Panel uses Midnight Glass aesthetic (backdrop-filter, dark glass background)
    - No layout breakage when opening/closing Finance panel
    - Main chat is unaffected by Finance interactions
  </how-to-verify>
  <resume-signal>Type "approved" if all scenarios pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
All 6 FIN requirements verified by human through live browser interaction.
Test suite: `python -m pytest tests/test_finance*.py -v` is all green.
</verification>

<success_criteria>
- All 6 FIN requirements pass human verification
- No visual layout breakage
- Main chat session unaffected by Finance panel interactions
- Test suite all green
</success_criteria>

<output>
After completion, create `.planning/phases/02-financial-advisor/02-07-SUMMARY.md`
</output>

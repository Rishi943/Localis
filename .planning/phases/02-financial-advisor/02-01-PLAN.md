---
phase: 02-financial-advisor
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_finance_csv.py
  - tests/test_finance_upload.py
  - tests/test_finance_onboarding.py
  - tests/test_finance_db.py
  - tests/test_finance_chat.py
autonomous: true
requirements: [FIN-01, FIN-02, FIN-03, FIN-05, FIN-06]
must_haves:
  truths:
    - "Running `python -m pytest tests/test_finance*.py -v` exits with all tests SKIPPED or XFAIL — not ERROR"
    - "CSV parser unit tests exist with fixture data embedded (no file I/O)"
    - "Categoriser unit tests verify keyword matching and Other fallback"
    - "Deduplication test verifies silent INSERT OR IGNORE behaviour"
    - "Onboarding persistence test verifies fin_goals row and app_settings flag"
    - "Finance chat test verifies SQL context block is injected into system prompt"
  artifacts:
    - path: "tests/conftest.py"
      provides: "Shared fixtures: in-memory SQLite DB, FastAPI test client"
    - path: "tests/test_finance_csv.py"
      provides: "FIN-02/FIN-03 unit tests: chequing parser, credit card parser, categoriser"
    - path: "tests/test_finance_upload.py"
      provides: "FIN-02 integration tests: upload endpoint, deduplication"
    - path: "tests/test_finance_onboarding.py"
      provides: "FIN-01 integration tests: goals persistence, reset"
    - path: "tests/test_finance_db.py"
      provides: "FIN-05 integration tests: multi-period aggregation"
    - path: "tests/test_finance_chat.py"
      provides: "FIN-06 integration tests: SQL context injection"
  key_links:
    - from: "tests/conftest.py"
      to: "tests/test_finance*.py"
      via: "pytest fixtures"
      pattern: "def fin_db\\|def client"
---

<objective>
Create the test scaffold for Phase 2. All tests are written RED (failing/skipping) before any implementation code exists. This gives Wave 1+ tasks a clear verification target and ensures the test infrastructure is stable before production code is built.

Purpose: Test-first contract. Each test file defines the exact interface that implementation plans must satisfy.
Output: 6 test files with pytest fixtures and failing/skipped test stubs covering all FIN-01..06 requirements.
</objective>

<execution_context>
@/home/rishi/.claude/get-shit-done/workflows/execute-plan.md
@/home/rishi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-financial-advisor/02-CONTEXT.md
@.planning/phases/02-financial-advisor/02-RESEARCH.md
@.planning/phases/02-financial-advisor/02-VALIDATION.md
</context>

<feature>
  <name>Finance Test Scaffold (Wave 0)</name>
  <files>tests/conftest.py, tests/test_finance_csv.py, tests/test_finance_upload.py, tests/test_finance_onboarding.py, tests/test_finance_db.py, tests/test_finance_chat.py</files>
  <behavior>
    CSV Parser tests (test_finance_csv.py):
    - test_chequing_debit: row ["01/15/2026","TIM HORTONS","5.75",""] → {type:"debit", amount:5.75, category:"Food"}
    - test_chequing_credit: row ["01/20/2026","E-TRANSFER","","200.00"] → {type:"credit", amount:200.0}
    - test_credit_card_debit: row ["01/10/2026","NETFLIX","15.99","","XXXX1234"] → {type:"debit", amount:15.99, category:"Entertainment"}
    - test_credit_card_payment: row ["01/25/2026","PAYMENT THANK YOU","500.00","","XXXX1234"] → {type:"credit"}
    - test_encoding_latin1: latin-1 bytes with é char → decoded without UnicodeDecodeError
    - test_account_type_detection: 4-col row → "chequing", 5-col row → "credit_card"
    - test_categorise_food: "TIM HORTONS YONGE ST" → "Food"
    - test_categorise_transport: "TTC PRESTO RELOAD" → "Transport"
    - test_categorise_other: "RANDOM MERCHANT XYZ" → "Other"

    Upload + dedup tests (test_finance_upload.py):
    - test_upload_endpoint: POST /finance/upload_csv with valid chequing CSV → 200, row_count > 0
    - test_dedup: same CSV uploaded twice → second upload returns inserted=0 or row_count matches skipped_count

    Onboarding tests (test_finance_onboarding.py):
    - test_goals_persist: POST /finance/goals with {goal_type, life_events, budgets, horizon} → DB row exists, fin_onboarding_done="true"
    - test_reset: POST /finance/reset_goals → fin_goals empty, fin_onboarding_done="false"

    DB aggregation tests (test_finance_db.py):
    - test_multi_period_aggregation: insert rows with period_label="Jan 2026" and "Feb 2026" → /finance/dashboard?period=All+time returns combined totals

    Finance chat tests (test_finance_chat.py):
    - test_sql_context_injection: POST /finance/chat with message → response contains "FINANCIAL CONTEXT" prefix in system prompt (verify via mock or response structure)
  </behavior>
  <implementation>
    Write tests RED — they will fail with ImportError or 404 since app/finance.py does not exist yet.
    Use `pytest.mark.xfail(strict=False)` or `pytest.skip()` on tests that require the live app endpoint until implementation exists.
    For pure unit tests (parser, categoriser), use MISSING import guard: `try: from app.finance import parse_chequing_csv, ... except ImportError: pytest.skip("finance module not yet implemented")`.
    For integration tests, use the FastAPI TestClient with conftest `client` fixture.

    conftest.py must provide:
    - `fin_db` fixture: in-memory SQLite with fin_* tables created (call database.init_db with temp path)
    - `client` fixture: FastAPI TestClient around the main app
    - Fixture CSV strings: CHEQUING_CSV_FIXTURE (3 debit + 1 credit row), CREDIT_CARD_CSV_FIXTURE (2 spend + 1 payment row)
  </implementation>
</feature>

<verification>
Run: `python -m pytest tests/test_finance*.py -v 2>&1 | tail -20`
Expected: All tests collected, all pass/skip/xfail — no ERROR or unexpected FAILED.
The test suite must run in under 15 seconds.
</verification>

<success_criteria>
`python -m pytest tests/test_finance*.py -v` completes with 0 ERRORs.
All tests are either PASSED, SKIPPED, or XFAIL (not hard FAILED on infrastructure issues).
Test file count: 6. Total test count: at least 13.
</success_criteria>

<output>
After completion, create `.planning/phases/02-financial-advisor/02-01-SUMMARY.md`
</output>

---
phase: 02.1
plan: 06
type: execute
wave: 5
depends_on: [02.1-05, 02.1-03]
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "All 9 tests pass (pytest tests/test_notes.py tests/test_notes_e2e.py -v)"
    - "Notes RSB icon visible and clickable in browser"
    - "Notes dashboard opens as full-screen overlay on icon click"
    - "Voice command creates note and it appears in grid"
    - "Reminder with past due_at shows amber pulse in grid"
  artifacts:
    - path: "tests/test_notes.py"
      provides: "6 unit tests — all passing"
    - path: "tests/test_notes_e2e.py"
      provides: "3 integration tests — all passing"
---

<objective>
Human verification that the full Notes and Reminders feature works correctly: backend API, voice creation, dashboard UI, card interactions, and reminder delivery.

Purpose: Sign off on Phase 02.1 before marking it complete.
Output: Human confirms all 5 functional areas work; phase is marked done.
</objective>

<execution_context>
@/home/rishi/.claude/get-shit-done/workflows/execute-plan.md
@/home/rishi/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02.1-notes-and-reminders-voice-triggered-google-keep-style-notepad-with-timed-reminder-pings/02.1-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and verify pass</name>
  <read_first>
    - (no files to read — run tests and report results)
  </read_first>
  <files></files>
  <action>
Run the full notes test suite and fix any failures before proceeding to the human checkpoint:

```bash
cd /home/rishi/Rishi/AI/Localis
python -m pytest tests/test_notes.py tests/test_notes_e2e.py -v 2>&1
```

If any test fails, diagnose the failure and fix the underlying code (not the test), then re-run until all 9 pass. Do not mark this task done until the command exits 0.

Also run a quick JS syntax check:
```bash
node --check app/static/js/app.js && echo "JS syntax OK"
```

Fix any JS syntax errors before proceeding.
  </action>
  <verify>
    <automated>cd /home/rishi/Rishi/AI/Localis && python -m pytest tests/test_notes.py tests/test_notes_e2e.py -v 2>&1 | tail -5 && node --check app/static/js/app.js && echo "All checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_notes.py tests/test_notes_e2e.py -v` exits 0 with 9 passed
    - `node --check app/static/js/app.js` exits 0 with no errors
    - No FAILED or ERROR lines in pytest output
  </acceptance_criteria>
  <done>9 tests pass; JS has no syntax errors. Ready for human checkpoint.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 02.1 — Notes and Reminders:
- Backend: POST /notes/add, GET /notes/list, GET /notes/due, PATCH /notes/{id}, DELETE /notes/{id}, POST /notes/dismiss/{id}
- Tool pipeline: notes.add and notes.retrieve tools in ALLOWED_TOOLS + execute_tool branches
- Frontend: Notes RSB icon, full-screen grid dashboard, 6 card color tints, overdue amber pulse, create card, click-to-edit, delete with undo toast
- Reminder polling: every 30s, chime sound, Web Notifications
  </what-built>
  <how-to-verify>
Start the app: `uvicorn app.main:app --reload --host 0.0.0.0 --port 8000`

Then verify each area:

**1. Notes icon and dashboard (30s)**
- Look at the right sidebar icon strip — a notepad icon should appear
- Click it: notes dashboard should open as full-screen overlay
- Dashboard shows empty state: "No notes yet"
- Press Escape — dashboard closes

**2. Create a note via UI (30s)**
- Open notes dashboard
- Click the dashed "Add a note..." card
- Type: "Test note from UI" and press Enter or click outside
- New note card appears in grid with default (dark glass) color
- Close and reopen — note persists

**3. Edit and delete via UI (30s)**
- Click an existing note card: textarea appears with current content
- Change text, click outside: card updates
- Hover a card: trash icon appears
- Click trash: card fades, toast appears "Note deleted. [Undo] [Dismiss]"
- Either click Undo (card restores) or wait 5s (card removed)

**4. Voice note creation (60s)**
- Enable wakeword or use text chat in assist mode
- Type or say: "add note: test the notes feature works"
- Response should say "Got it. Note saved."
- Open notes dashboard — new note card visible

**5. Reminder (60s)**
- Type in chat: "remind me to check the notes dashboard in 1 minute"
- Response confirms reminder
- Wait ~1 minute — chime should play + browser notification fires (if granted) + amber pulse on card

**6. Reminder overdue state (10s)**
- Open notes dashboard
- Any reminder with past due_at should show amber pulsing border and ⏰ badge
  </how-to-verify>
  <resume-signal>Type "approved" if all 6 areas work correctly. Or describe any issues found (e.g., "badge not updating", "create card not saving") and the executor will fix them.</resume-signal>
</task>

</tasks>

<verification>
Phase 02.1 complete when:
1. `pytest tests/test_notes.py tests/test_notes_e2e.py -v` exits 0 (9 passed)
2. Human confirms: notes icon visible, dashboard opens, voice creates note, reminder fires
</verification>

<success_criteria>
- 9 automated tests pass
- Human signs off on all 6 functional verification areas
- Phase 02.1 marked complete in ROADMAP.md
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-notes-and-reminders-voice-triggered-google-keep-style-notepad-with-timed-reminder-pings/02.1-06-SUMMARY.md`

Update ROADMAP.md Phase 02.1 status to complete.
Update CLAUDE.md Changelog with Phase 02.1 entry.
Update .planning/STATE.md stopped_at to "Phase 02.1 complete".
</output>
